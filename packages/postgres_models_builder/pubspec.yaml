name: postgres_models_builder
description: Generate dart type definitions from PostgreSQL database schema.
version: 0.3.4
homepage: https://hippolabs.org
publish_to: "https://registry.hippolabs.org/pub/internal-pub/"
resolution: workspace

environment:
  sdk: ">=3.11.0 <4.0.0"

dependencies:
  args: ^2.7.0
  built_collection: ^5.1.1
  cli_util: ^0.4.2
  code_builder: ^4.11.1
  dart_style: ^3.1.5
  meta: ^1.17.0
  path: ^1.9.1
  postgres: ^3.5.9

dev_dependencies:
  lints: ^6.1.0
  test: any

executables:
  postgres-models-builder: postgres_models_builder
