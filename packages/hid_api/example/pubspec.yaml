name: hid_api_example
description: "Demonstrates how to use the hid_api plugin."
publish_to: "none"
resolution: workspace

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter

  hid_api:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
