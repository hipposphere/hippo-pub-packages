name: hippo_native_deps
description: Shared native dependency metadata for Hipposphere packages.
version: 0.1.1
homepage: https://hippolabs.org
publish_to: "https://registry.hippolabs.org/pub/internal-pub/"
resolution: workspace

environment:
  sdk: ">=3.12.0 < 4.0.0"

dependencies:
  code_assets: ^1.2.1
  hooks: ^2.0.2

dev_dependencies:
  lints: ^6.1.0
