## 0.3.1

- Resolve shared speech packages from the Hippo internal registry.

## 0.3.0

- Add the independent endorsed macOS AVFoundation/CoreAudio recorder with its
  persistent control worker.
- Move macOS AAC, metadata, native sources, generated bindings, and build hooks
  out of `speech_utils`.
