name: app_release_client
description: App release client package for channel selection and appcast URL resolution.
version: 0.1.9
homepage: https://hippolabs.org
publish_to: "https://registry.hippolabs.org/pub/internal-pub/"
resolution: workspace

environment:
  sdk: ">=3.12.0 < 4.0.0"
  flutter: ">=3.44.0"

dependencies:
  flutter:
    sdk: flutter
  hippo_utils:
    hosted: https://registry.hippolabs.org/pub/internal-pub/
    version: ^0.3.2
  hippo_core:
    hosted: https://pub.hippolabs.org
    version: ^0.1.12
  hippo_core_flutter:
    hosted: https://pub.hippolabs.org
    version: ^0.1.13
  chopper: ^8.6.0
  json_annotation: ^4.12.0
  collection: ^1.19.1
  http: ^1.6.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  swagger_dart_code_generator: ^4.1.1
  build_runner: ^2.15.1
  chopper_generator: ^8.6.2
  json_serializable: ^6.13.2
