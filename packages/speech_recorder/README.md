# speech_recorder

Speech recorder abstractions for Hipposphere Flutter applications.

This package wraps the shared `speech_utils` native recorder APIs with a
Flutter-oriented session layer for file recording, streaming, segmented capture,
and manual segment splitting.
