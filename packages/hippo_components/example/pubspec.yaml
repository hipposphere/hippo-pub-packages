name: hippo_components_example
description: Example app for AdaptiveDetailContainer.
publish_to: 'none'
version: 1.0.0+1
resolution: workspace

environment:
  sdk: ">=3.12.0 < 4.0.0"
  flutter: ">=3.44.0"

dependencies:
  flutter:
    sdk: flutter
  hippo_components:
    path: ../
  hippo_utils:
    path: ../../hippo_utils

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
