name: hippo_core_flutter
description: Flutter bindings and storage implementations for hippo_core primitives.
version: 0.1.1
homepage: https://hippolabs.org
publish_to: "https://registry.hippolabs.org/pub/internal-pub/"
resolution: workspace

environment:
  sdk: ">=3.11.0 < 4.0.0"
  flutter: ">=3.41.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_secure_storage: ^10.1.0
  hippo_core:
    hosted: https://registry.hippolabs.org/pub/internal-pub/
    version: ^0.1.1
  rxdart: ^0.28.0
  shared_preferences: ^2.5.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
