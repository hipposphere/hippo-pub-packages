name: hippo_packages
description: A monorepo for Hippo packages.
publish_to: none
homepage: https://hippolabs.org
version: 0.1.0

environment:
  sdk: ">=3.11.0 <4.0.0"
dev_dependencies:
  melos: ^7.4.0

workspace:
  - packages/analytics_posthog
  - packages/dictation_support
  - packages/dictation_support/example
  - packages/hid_api
  - packages/hid_api/example
  - packages/hippo_auth
  - packages/hippo_auth/example
  - packages/app_release_client
  - packages/app_release_client/example
  - packages/hippo_bluetooth_client
  - packages/hippo_bluetooth_client_builder
  - packages/hippo_bluetooth_client/example
  - packages/hippo_components
  - packages/hippo_ui
  - packages/hippo_utils
  - packages/hotkey_api
  - packages/hotkey_api/example
  - packages/postgres_base_models
  - packages/postgres_models_builder
  - packages/desktop_autopaste
  - packages/desktop_autopaste/example
  - packages/hippo_native_deps
  - packages/app_permissions
  - packages/app_permissions/example
  - packages/speech_recorder
  - packages/speech_recorder/example
  - packages/speech_utils
  - packages/speech_utils/example

melos:
  ide:
    intellij:
      enabled: false
  scripts:
    format:
      run: dart format .
      description: Format all Dart files in the repository
    check-format:
      run: dart format --set-exit-if-changed .
      description: Check if all Dart files are formatted
    analyze:
      run: dart analyze
      description: Run Dart analyzer to check for lint errors
    test:
      run: melos exec --dir-exists=test -- flutter test
      description: Run all tests
    ci:
      run: |
        melos run check-format
        melos run analyze
        melos run test
      description: Check formatting, analyze for lint errors, and run tests
    # Not required for now, but useful to have in the future
    license-add-headers:
      run: npx -y license-check-and-add add
      description: "Add/update license headers across the repo"
    license-check-headers:
      run: npx -y license-check-and-add check
      description: "Fail if headers are missing (CI-friendly)"
