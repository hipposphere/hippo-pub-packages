/// Metadata key emitted by `hook/build.dart` for the RapidJSON include path.
const rapidjsonIncludeDirMetadataKey = 'rapidjson_include_dir';

/// Metadata key emitted by `hook/build.dart` for the vendored RapidJSON version.
const rapidjsonVersionMetadataKey = 'rapidjson_version';

/// Metadata key emitted by `hook/build.dart` for the WIL include path.
const wilIncludeDirMetadataKey = 'wil_include_dir';

/// Metadata key emitted by `hook/build.dart` for the vendored WIL version.
const wilVersionMetadataKey = 'wil_version';
