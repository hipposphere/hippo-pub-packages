# speech_utils_macos

Independent macOS AVFoundation/CoreAudio recorder implementation for the
federated `speech_utils` family. It owns its recorder lifecycle and native
assets; it does not share iOS audio-session state.
