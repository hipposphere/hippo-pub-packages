# desktop_autopaste_linux

Endorsed Linux implementation for the federated `desktop_autopaste` package.
It owns the X11/XWayland clipboard selection, XTEST shortcut injection, native
bindings, build hook, and code asset.

Linux builds require the X11 and Xtst development libraries.
