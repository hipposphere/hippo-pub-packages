# hid_api_linux

Endorsed Linux implementation for the federated `hid_api` package. It owns
the hidapi-backed native plugin and registers the shared method-channel
adapter.

Linux builds require the `hidapi-hidraw` development package.
