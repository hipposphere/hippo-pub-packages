name: hippo_auth
description: Flutter package for Hippo authentication
version: 0.0.7
homepage: https://hippolabs.org
publish_to: "https://registry.hippolabs.org/pub/internal-pub/"
resolution: workspace

environment:
  sdk: ^3.9.2
  flutter: ">=3.35.0"

dependencies:
  flutter_web_auth_2: ^5.0.0-alpha.7
  flutter:
    sdk: flutter
  hippo_utils:
    hosted: https://registry.hippolabs.org/pub/internal-pub/
    version: ^0.1.5
  chopper: ^8.4.0
  json_annotation: ^4.9.0
  collection: ^1.19.1
  http: ^1.5.0
  hippo_components:
    hosted: https://registry.hippolabs.org/pub/internal-pub/
    version: ^0.1.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  swagger_dart_code_generator: ^4.0.2
  build_runner: any
  chopper_generator: ^8.4.0
  json_serializable: ^6.11.1

