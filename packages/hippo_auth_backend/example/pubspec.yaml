name: hippo_auth_backend_example
description: An example application for the Hippo authentication backend.
version: 0.1.0
publish_to: none
resolution: workspace

environment:
  sdk: ">=3.12.0 < 4.0.0"

dependencies:
  hippo_auth_backend:
    path: ../
  dart_http_server:
    hosted: https://pub.hippolabs.org
    version: ^0.3.26
  dart_sql:
    hosted: https://pub.hippolabs.org
    version: ^0.4.1
  json_schema:
    hosted: https://pub.hippolabs.org
    version: ^0.1.1
  
