name: speech_utils_platform_interface
description: Platform backend contract for the federated speech_utils package family.
version: 0.3.0
homepage: https://hippolabs.org
publish_to: "https://registry.hippolabs.org/pub/internal-pub/"
resolution: workspace

environment:
  sdk: ">=3.12.0 <4.0.0"

dependencies:
  ffi: ^2.2.0
  speech_utils_core: ^0.3.0

dev_dependencies:
  lints: ^6.1.0
  test: ^1.31.1
