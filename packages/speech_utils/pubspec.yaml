name: speech_utils
description: Utilities for PCM16 VAD segmentation and AAC compression.
version: 0.1.3
homepage: https://hippolabs.org
publish_to: "https://registry.hippolabs.org/pub/internal-pub/"
resolution: workspace

environment:
  sdk: ">=3.11.0 <4.0.0"

dependencies:
  code_assets: ^1.0.0
  ffi: ^2.2.0
  hooks: ^1.0.1
  path: ^1.9.1

dev_dependencies:
  ffigen: ^20.1.1
  lints: ^6.1.0
  test: ^1.26.0
