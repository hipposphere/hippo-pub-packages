## 0.3.1

- Resolve shared speech packages from the Hippo internal registry.

## 0.3.0

- Add the endorsed Android implementation using the existing AudioRecord and
  MediaRecorder workers.
- Move Android permissions, JNI bindings, MediaCodec AAC, metadata support,
  native assets, and generation tooling out of `speech_utils`.
