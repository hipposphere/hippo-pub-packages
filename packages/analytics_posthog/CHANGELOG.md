## 0.2.3

- Update `posthog_flutter` to `^5.28.0`.
- Raise the minimum `hippo_utils` dependency to `^0.3.3`.

## 0.1.0

- Initial release.
- Adds `PosthogAnalyticsProvider` extracted from `hippo_utils`.
