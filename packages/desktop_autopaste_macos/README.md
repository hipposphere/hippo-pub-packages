# desktop_autopaste_macos

Endorsed macOS implementation for the federated `desktop_autopaste` package.
It owns the AppKit/ApplicationServices implementation, Swift source, native
bindings, build hook, and code asset.
