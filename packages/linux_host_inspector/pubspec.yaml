name: linux_host_inspector
description: Typed, read-only Linux host and process inspection through procfs, sysfs, and libc.
version: 0.1.0
homepage: https://hippolabs.org
publish_to: "https://registry.hippolabs.org/pub/internal-pub/"
resolution: workspace

environment:
  sdk: ">=3.12.0 <4.0.0"

dependencies:
  ffi: ^2.1.4

dev_dependencies:
  test: ^1.31.1
