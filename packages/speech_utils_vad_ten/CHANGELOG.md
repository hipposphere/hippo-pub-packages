## 0.3.1

- Resolve `speech_utils_core` from the Hippo internal registry.

## 0.3.0

- Add the TEN VAD provider with native binaries, generated FFI bindings,
  platform asset selection, version reporting, and handle lifecycle.
