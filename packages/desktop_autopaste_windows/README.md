# desktop_autopaste_windows

Endorsed Windows implementation for the federated `desktop_autopaste` package.
It owns clipboard publication/restoration, keyboard injection, focused-field
context and editing, native bindings, build hook, and code asset.
