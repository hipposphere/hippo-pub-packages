# speech_utils_android

Endorsed Android implementation for `speech_utils`. It owns the existing
`AudioRecord` streaming recorder, `MediaRecorder` AAC file path, permission
integration, generated JNI bindings, and NDK MediaCodec AAC asset.

This package does not use the `record` package family.
