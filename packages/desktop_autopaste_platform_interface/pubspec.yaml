name: desktop_autopaste_platform_interface
description: Platform contract for the federated desktop_autopaste package family.
version: 0.3.0
homepage: https://hippolabs.org
publish_to: "https://registry.hippolabs.org/pub/internal-pub/"
resolution: workspace

environment:
  sdk: ">=3.12.0 <4.0.0"

dependencies:
  ffi: ^2.2.0

dev_dependencies:
  lints: ^6.1.0
  test: ^1.31.1
